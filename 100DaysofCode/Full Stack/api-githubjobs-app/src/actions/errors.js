export const setErrors = (error) => ({
    type: 'SET_ERRORS',
    error
  });
  
  export const resetErrors = () => ({
    type: 'RESET_ERRORS'
  });

//   For calling dispatch actions to the reducer
